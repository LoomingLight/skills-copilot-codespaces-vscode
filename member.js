function skillsMember() {
  // Get the member object
  var member = this;
  // Get the skills object
  var skills = member.skills;
  // Get the skills object keys
  var skillsKeys = Object.keys(skills);
  // Get the skills object values
  var skillsValues = Object.values(skills);
  // Get the skills object entries
  var skillsEntries = Object.entries(skills);
  // Get the skills object prototype
  var skillsPrototype = Object.getPrototypeOf(skills);
  // Get the skills object class
  var skillsClass = skills.constructor;
  // Get the skills object name
  var skillsName = skills.constructor.name;
  // Get the skills object prototype keys
  var skillsPrototypeKeys = Object.keys(skillsPrototype);
  // Get the skills object prototype values
  var skillsPrototypeValues = Object.values(skillsPrototype);
  // Get the skills object prototype entries
  var skillsPrototypeEntries = Object.entries(skillsPrototype);
  // Get the skills object prototype constructor
  var skillsPrototypeConstructor = skillsPrototype.constructor;
  // Get the skills object prototype isPrototypeOf
  var skillsPrototypeIsPrototypeOf = skillsPrototype.isPrototypeOf(skills);
  // Get the skills object prototype propertyIsEnumerable
  var skillsPrototypePropertyIsEnumerable = skillsPrototype.propertyIsEnumerable('name');
  // Get the skills object prototype hasOwnProperty
  var skillsPrototypeHasOwnProperty = skillsPrototype.hasOwnProperty('name');
  // Get the skills object prototype toString
  var skillsPrototypeToString = skillsPrototype.toString();
  // Get the skills object prototype valueOf
  var skillsPrototypeValueOf = skillsPrototype.valueOf();
  // Get the skills object prototype toLocaleString
  var skillsPrototypeToLocaleString = skillsPrototype.toLocaleString();
  // Get the skills object prototype keys
  var skillsPrototypeKeys = skillsPrototype.keys();
  // Get the skills object prototype values
  var skillsPrototypeValues = skillsPrototype.values();
  // Get the skills object prototype entries
  var skillsPrototypeEntries = skillsPrototype.entries();
  // Get the skills object prototype getPrototypeOf
  var skillsPrototypeGetPrototypeOf = Object.getPrototypeOf(skillsPrototype);
  // Get the skills object prototype setPrototypeOf
  var skillsPrototypeSetPrototypeOf = Object.setPrototypeOf(skillsPrototype, {});
  // Get the skills object prototype defineProperty
  var skillsPrototypeDefineProperty = Object.defineProperty(skillsPrototype, 'name', { value: 'skills' });
  // Get the skills object prototype define